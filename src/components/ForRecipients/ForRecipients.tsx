import React, { FC } from 'react'
import styles from './ForRecipients.module.css'

export const ForRecipients: FC = () => {
  const { container } = styles
  return <div className={container}>FOR RECIPIENTS</div>
}
